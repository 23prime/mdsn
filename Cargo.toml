[package]
name = "mdsn"
version = "0.3.1"
edition = "2024"

[dependencies]
anyhow = "1"
clap = { version = "4", features = ["derive"] }
glob = "0.3"
ignore = "0.4"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
self_update = { version = "1.0", default-features = false, features = [
    "reqwest",
    "github",
    "archive-tar",
    "archive-zip",
    "flate2",
    "rustls",
] }
